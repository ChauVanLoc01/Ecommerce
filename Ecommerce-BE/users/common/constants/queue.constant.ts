export enum QueueAction {
  sendMail = 'sendMail',
  register = 'register',
  forgetPassword = 'forgotPassword',
  addToCache = 'addToCache'
}

export enum QueueName {
  mail = 'sendMail',
  product = 'product'
}
